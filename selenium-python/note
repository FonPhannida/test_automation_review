create main folder
create file "download drive"
install webdriver_manager >>
    pip install webdriver-manager
install pytest >>
    pip install pytest
    pytest
    pip show pytest

create folder to install driver >> drivers
run pytest utils/download_chrome_driver.py >> get file : drivers/chromedriver
set  directory in .env

go to file > conftest.py to set driver
install selenium into project
import load_dotenv >>
code about calling driver

go to file base page

